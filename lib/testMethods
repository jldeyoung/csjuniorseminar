import 'package:flutter/material.dart';

// insert methods here

bool eliminationSuccessful() {
  /*
    Random number generator between [1,# of icebreaker questions]
    Pass that as the question/answer we want to retrieve
    Call a method that displays a prompt to the user [askUser(questionID)]
        Get the answer to that question
    Compare the strings(ignore case)
        Return string equality
   */
  return true;//VERSION 0 ONLY!!!
}